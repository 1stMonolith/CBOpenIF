package type

import "../com"
import "../bstr"
import "../variant"
import "../enumtypes"

@(private) HResult     :: com.HResult
@(private) BStr        :: bstr.BStr
@(private) GUID        :: com.GUID
@(private) VariantBool :: variant.VariantBool
@(private) Scope       :: enumtypes.ScopeType